<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= pageTitle || 'My Site' %></title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">Assignment02</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <% if (!authenticated) { %>
          <li class="nav-item">
            <a class="nav-link <%= activeTab==='signup' ? 'active' : '' %>" href="/signup">Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= activeTab==='login' ? 'active' : '' %>" href="/login">Log In</a>
          </li>
          <% } else { %>
          <li class="nav-item">
            <a class="nav-link <%= activeTab==='members' ? 'active' : '' %>" href="/members">Members</a>
          </li>
          <% if (user_type==='admin') { %>
          <li class="nav-item">
            <a class="nav-link <%= activeTab==='admin' ? 'active' : '' %>" href="/admin">Admin</a>
          </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Log Out</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container">
